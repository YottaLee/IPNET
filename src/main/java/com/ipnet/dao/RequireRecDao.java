package com.ipnet.dao;

import com.ipnet.entity.Rec.RequireRec;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequireRecDao extends JpaRepository<RequireRec,String> {
}
